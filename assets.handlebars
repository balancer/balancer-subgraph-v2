// This file is automatically generated and contains assets from {{network}}.
// Generate for other networks by running: yarn generate-assets [network].
// Supported networks are: arbitrum, goerli, mainnet, and polygon.

import { Address, Bytes } from '@graphprotocol/graph-ts';

class Assets {
  public stableAssets: Bytes[];
  public pricingAssets: Bytes[];
  public fxAssets: Address[];
  public fxAggregators: Address[];
}

{{#each stableAssets}}
export const {{symbol}}_ADDRESS = Bytes.fromHexString('{{address}}');
{{/each}}

export const assets: Assets = {
  stableAssets: [
    {{#each stableAssets}}
    Bytes.fromHexString('{{address}}'), // {{symbol}}
    {{/each}}
  ],
  pricingAssets: [
    {{#each pricingAssets}}
    Bytes.fromHexString('{{address}}'), // {{symbol}}
    {{/each}}
  ],
  fxAssets: [
    {{#each fxAssets}}
    Address.fromString('{{address}}'), // {{symbol}}
    {{/each}}
  ],
  fxAggregators: [
    {{#each fxAggregators}}
    Address.fromString('{{address}}'), // {{symbol}}
    {{/each}}
  ],
};
