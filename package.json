{
  "name": "balancer",
  "version": "0.1.0",
  "scripts": {
    "create": "graph create balancer-labs/balancer-v2 --node https://api.thegraph.com/deploy/",
    "create:local": "graph create balancer-labs/balancer-v2 --node http://127.0.0.1:8020",
    "codegen": "graph codegen --output-dir src/types/",
    "build": "graph build",
    "lint": "eslint . --ext .ts",
    "clean": "docker-compose -f docker-compose.yml -f docker-compose.hardhat.yml kill && docker-compose -f docker-compose.yml -f docker-compose.hardhat.yml rm -f &&  sudo rm -rf data",
    "start:hardhat": "docker-compose -f docker-compose.yml -f docker-compose.hardhat.yml up",
    "clean:parity": "docker-compose kill && docker-compose rm -f &&  sudo rm -rf data",
    "start:parity": "docker-compose up",
    "restart:parity": "yarn clean:parity && yarn start:parity",
    "deploy": "graph deploy balancer-labs/balancer-v2 --ipfs https://api.thegraph.com/ipfs/ --node https://api.thegraph.com/deploy/",
    "deploy:kovan": "graph deploy balancer-labs/balancer-kovan-v2 subgraph.kovan.yaml --ipfs https://api.thegraph.com/ipfs/ --node https://api.thegraph.com/deploy/",
    "deploy:local": "graph deploy balancer-labs/balancer-v2 subgraph.yaml --debug --ipfs http://localhost:5001 --node http://127.0.0.1:8020",
    "import-abis": "cp ./core/abi/* ./abis/.",
    "update-addresses": "tsc ./scripts/update-addresses-subgraph.ts && node ./scripts/update-addresses-subgraph.js",
    "update-addresses:kovan": "tsc ./scripts/update-addresses-subgraph.ts && node ./scripts/update-addresses-subgraph.js kovan",
    "sync": "yarn import-abis && yarn update-addresses && yarn codegen && yarn build",
    "sync:kovan": "yarn import-abis && yarn update-addresses kovan && yarn codegen && yarn build"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.18.0",
    "@graphprotocol/graph-ts": "^0.18.1",
    "@typescript-eslint/eslint-plugin": "^4.1.1",
    "@typescript-eslint/parser": "^4.1.1",
    "assemblyscript": "^0.17.5",
    "eslint": "^7.9.0",
    "eslint-plugin-mocha-no-only": "^1.1.1",
    "eslint-plugin-prettier": "^3.1.4",
    "js-yaml": "^4.0.0",
    "prettier": "^2.1.2",
    "typescript": "^4.0.2"
  },
  "dependencies": {
    "@assemblyscript/loader": "^0.17.5",
    "babel-polyfill": "^6.26.0",
    "babel-register": "^6.26.0"
  }
}
